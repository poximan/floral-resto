<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de la aplicacion -->
    <style name="Theme.Resto_app" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema para el Splash Screen (API 31+) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/mobile_splash_logo</item>
        <item name="windowSplashScreenBackground">#FFF1DF</item>
        <item name="postSplashScreenTheme">@style/Theme.Resto_app</item>
    </style>
</resources>
